# 进程执行器规范

## 1. 目标

进程执行器用于让 Honeycomb 通过外部进程执行某些工作单元，主要作为兼容层或过渡层。

## 2. 作用边界

它属于执行面，不属于进化面。

## 3. 建议输入输出方式

建议使用结构化标准输入输出：

- 输入：结构化请求
- 输出：结构化结果
- 错误：显式错误对象或退出码

## 4. 当前建议

在新的蜂后与工蜂模型下，进程执行器更适合作为兼容路径，而不是长期主路径。
